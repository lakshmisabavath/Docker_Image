apiVersion: v1
kind: Service
metadata:
  name: kubedemoapp-service
spec:
  selector:
    app: kuberdemoapp
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodeport: 30080

  type: Nodeport
